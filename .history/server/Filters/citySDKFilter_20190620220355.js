const vars = require('../Variables')

const methods = {

    filterPopulation(data) {
       
        console.log(data[0].NAME)
        console.log(data[0].B01002_001E)
    }
}

module.exports = methods